export const meta = {
  name: 'tokium-gh-verify-wave',
  description: 'TOKIUM直アポ大 G/H列の検証（1波ぶん。args={wave,tier,blocks,cost}）',
  phases: [
    { title: 'Verify', detail: '担当行をブロック並列で1次検証' },
    { title: 'Adversarial', detail: '○維持と判定した行を反証しにいく' },
  ],
}

const SP = '/tmp/claude-0/-home-user--/fcda6a82-279b-5a30-a006-b52ce02ba704/scratchpad'
const RULES = '/home/user/-/tokium/RULES.md'

const W = args.wave
const TIER = args.tier
const NB = args.blocks
const COST = args.cost
const ws = String(W).padStart(2, '0')

const SCHEMA = {
  type: 'object',
  required: ['block', 'rows_written', 'flipped_rows', 'kept_ok_rows', 'thin_margin_rows', 'notes'],
  properties: {
    block: { type: 'integer' },
    rows_written: { type: 'integer' },
    flipped_rows: { type: 'array', items: { type: 'integer' } },
    kept_ok_rows: { type: 'array', items: { type: 'integer' } },
    thin_margin_rows: { type: 'array', items: { type: 'integer' },
      description: '○のうち、採用値が450億円未満か、数値・決算期・親子判定に疑いが残る行' },
    unverified_rows: { type: 'array', items: { type: 'integer' },
      description: '検索枠切れ等で実際には検証できなかった行' },
    searches_used: { type: 'integer' },
    notes: { type: 'string' },
  },
}
const ADV_SCHEMA = {
  type: 'object',
  required: ['block', 'overturned', 'summary'],
  properties: {
    block: { type: 'integer' },
    overturned: { type: 'array', items: { type: 'integer' } },
    summary: { type: 'string' },
  },
}

const CONSTRAINTS = `## この環境の実測された制約（プローブ済み。再確認に時間を使うな）
- **WebFetch は全ドメインが egress ポリシーで遮断されている。呼ぶな。** bash の curl も同じ。
- 使えるのは **WebSearch のみ**。allowed_domains パラメータは効く。
- **このワークフロー実行全体で WebSearch は200回まで。** ${NB}エージェントで分け合う。
  1行あたり **平均${COST}クエリ** が予算だが、**均等に配るな。トリアージしろ。**
  **1次検証は全体の2/3までに抑えろ。** 残りは後段の反証に要る。

### 検索予算のトリアージ（重要）
全行に同じ手間をかけると薄い行に予算が回らない。次の順で配分しろ:
- **0クエリで済ませる行**: 採用値が1,000億円超で、出典が決算短信/有価証券報告書/EDINET/
  TDnetのように既に強く、親子判定にも疑いがない行。桁の検算だけして○にせよ。
  3,000億円の行を検索しても300億の判定は動かない。
- **1クエリ**: 500億〜1,000億円で出典が自社HPの行。数値の再現を1回試す。
- **2〜4クエリ**: **採用値が450億円未満**、子会社なのに連結/グループ値、出典がcatr.jp単独、
  決算期が不明・古い、出典URLが怪しい行。ここに予算を集中させろ。
  基準300億に近い行ほど、数値が少し違うだけで判定が反転する。
  検索枠が尽きて検証できなかった行は、○を付けず I列を
  「未検証（検索枠切れ）：〜」とし、unverified_rows に入れて報告しろ。
  **検証していない行に「○ 妥当」と書くな。** 成果物が検証済みを偽ることになる。
- WebSearch の危険な失敗様式（実測）: 見つからないとき「不明」ではなく
  **前年度の数値をもっともらしく返す**。常洋水産で2026年3月期398.64億円を4クエリ探して
  一度も出ず、2025年3月期388.39億円が自信ありげに返った。
  **必ず「何年何月期の値か」を確認し、決算期が一致しない値を根拠にするな。**
- allowed_domains でドメインを絞っても、返る要約がそのドメイン由来である保証はない。
  ドメイン限定は補助であって証跡ではない。`

const IS_T3 = String(TIER).indexOf('T3') === 0

const RULESET_T3 = `## この層の性質（重要・従来と逆向き）
担当する行は**すべて1次が「確認不可（△）」と判定した行**だ。§3-5により△は
「直アポ大に載せてはいけない」を意味する。つまりこれらは**既にリストから外れている**。

したがってお前の仕事は「○を疑う」ことではない。
**1次が諦めすぎて、架電できたはずの企業を落としていないかを回収すること**だ。

§1の通り、誤りには2方向ある:
- 実際はNGなのに○ → アポインターが無報酬。最も避けるべき
- **実際はOKなのに確認不可 → 架電できたはずの企業を失う。これも実害**
この層で潰すのは後者だ。

## やること
各行について、**1次が当たっていない承認ソースを探せ。** §5-3の9系統を順に潰せ:
1. 自社HPの 会社概要 / 企業情報 / 会社案内 / 数字で見る○○
2. 自社HPの IR（決算短信・有価証券報告書・決算説明資料・統合報告書・業績ハイライト）
3. EDINET（有価証券報告書・四半期報告書）
4. TDnet / 適時開示（決算短信）
5. **決算公告**（自社HPの電子公告ページ、または官報）※非上場の株式会社はここに載ることが多い
6. **親会社・ホールディングスのIR資料** ※子会社の場合、親の決算短信や有報の巻末
   「関係会社の状況」「主要な損益情報等」に子会社単体の財務諸表が載っている
7. 公的機関（金融庁、経済産業省、gBizINFO、自治体の企業情報）
8. 新聞社の記事（決算期と売上高が明記されているもの）
9. 自社ドメイン内の採用ページ（「数字で見る」「会社データ」等）

**§5-3の実績では 5（決算公告）と 6（親会社IR巻末）が最も効く。**
1次のI列に「当たった経路」が列挙されている行は、**そこに無い経路を狙え。**

## 下限証明（実額が無くても○にできる強力な手）
親会社の有価証券報告書「関係会社の状況」には、子会社の売上高が載っていなくても
**「売上高（連結会社相互間の内部売上高を除く）の連結売上高に占める割合が10%を超えている」**
という定型注記が付くことがある。これは**実額を知らなくても下限を与える**。

  親会社の連結売上高 × 10% > 300億円  ならば、その子会社は基準を満たすことが確定する。

実例: 日清食品は有報に単体実額の記載がないが、日清食品HD連結7,690億円に対し
10%超の注記があるため単体は769億円超と確定でき、300億基準は決定的にクリア。
同様に旭化成ホームズは旭化成連結約2.9兆円の10%超で単体2,900億円超と確定。

**この注記を見つけたら、実額が取れなくても○にできる。** I列には
「○ 回収：親会社◯◯の有報『関係会社の状況』に連結売上高の10%超と注記。
連結◯◯億円×10%＝◯◯億円>300億円のため基準充足が確定」と書け。
ただし連結×10%が300億に届かない場合は下限として使えないので注意。

## 検索予算のトリアージ（T3は回収見込みで配分を変えろ）
全行を同じ深さで掘ると枠が尽きて後続ブロックが1件も検索できなくなる。回収見込みで配分しろ。

### 最優先で切り捨てろ：§7-1 待ちの行は探すな
1次のI列に「catr.jp」「官報決算公告が唯一の出典」「§7-1」とある行、または
お前が1クエリ目でそう判明した行は、**それ以上探すな。** 検証済み行の実測で
**T3の約17%がこれ**であり、これらは検索では解決しない。先方（濱本さん）の回答待ちだ。
I列を「△ §7-1回答待ち：<金額>（<決算期>・<単体/連結>）を官報決算公告で確認済み。
catr.jp が承認ソースと認められれば○に回収可能」と書いて次の行に移れ。
**この見極めに使ってよいのは1クエリまで。** ここで枠を使い切ると、本当に回収できる行に
予算が回らなくなる。

### 残りの予算配分:
- **3〜4クエリ**: 回収の見込みが高い行。①上場企業か上場子会社（親会社のIR・有報巻末が使える）、
  ②親会社の連結売上に対し10%を超える規模（有報「関係会社の状況」の開示対象になる）、
  ③自社HPに電子公告・IRページが存在する、④1次が「この経路が残っている」と明記している。
- **1クエリ**: 非上場・小規模で、1次が既に §5-3 の主要経路を潰し尽くしている行。
  1回だけ未踏の経路を試し、出なければ △ で確定させろ。
- **0クエリ**: 1次が「決算公告は貸借対照表のみで売上高欄が存在しない」ことまで確認済みの行。
  この場合その会社に開示義務がなく、探しても出てこない。△ が確定した結論だ。

## それでも見つからなければ △ のままでよい
§3-5の通り、エビデンスが取れない場合は「載せない」が確定した結論だ。
**推測で○に戻すな。** 禁止ソース（就活会議/SalesNow/Baseconnect/マイナビ/doda/OpenWork等）
にしか数値がない場合は△が正しい。§6-6の加藤化学がその実例だ。

## I列の書式
- 「△ 妥当：確認不可で正しい。当たった経路＝〜」 1次の△を支持＝**この会社は架電しない**。
  **追加で当たった経路を必ず列挙**
- 「○ 回収：〜」  承認ソースで300億以上を確認できた＝**架電可に戻す**。
  数値・決算期・単体連結の別・出典URLを必ず書く

**この層で「○ 妥当」と書くな。** 「○」で始めてよいのは架電可に戻す「○ 回収」だけだ。
担当行はすべて元が△＝除外済みなので、「○ 妥当」は「除外が妥当」とも「架電可」とも読め、
下流で架電可に誤集計される。実際に加藤化学（§6-6が名指しで確認不可とした企業）が
この書き方のせいで架電可に化けかけた。**△を支持するなら必ず「△ 妥当」で始めろ。**
- 「× 要修正：〜」 300億未満、法人格NG、法人格消滅などが確定した
- 「△ 要再確認：〜」 承認ソースは見つかったが §7-1(catr.jp) 等の未決着論点に依存する

## J列
検証に使ったURL。セミコロン区切り。**存在しないURLを創作するな。**
`

const RULESET_OK = `## 判定の焦点（RULES.md が唯一の基準。必ず読め）
1. **子会社にグループ連結値を当てていないか（§3-3 / §6-4）** — 最頻の誤り。
   子会社は**単体**300億以上のみOK。グループ全体が超えていても子会社は一律NG。
   親会社/HDなら連結300億超でOK。まず親か子かを確定させろ。
2. **G列の値がグループ値でないか** — 自社サイトが「グループ売上高」と注記した値を
   単体として採用した実例がある（行38 ヤマウチ）。
3. **catr.jp 単独依存でないか（§7-1）** — 官報そのものではなく民間の二次サイト。
   先方の承認可否が未確定。代替の承認ソースを当たり、なければ
   「△ 要再確認：catr.jp単独。§7-1の先方確認待ち」。
4. **決算期の鮮度（§3-8）** — 直近3期以内か。
5. **桁（§6-1）** — 41,266百万円=412.66億円、1兆223億円=10,223億円。必ず検算。
6. **同名企業（§6-2）** — 法人番号・所在地で照合。別会社の数字は空欄より危険。
7. **法人格（§3-6）** — 株式会社のみ。有限会社・合同会社ほかは一律NG。
8. **法人格の存否（§6-7）** — 合併で消滅していないか。商号変更していないか。
   実例: J.フロント建装は2026年3月にJ.フロントプライムスペースへ統合され消滅（新設法人＝別法人番号）。
   売上が基準を満たしても、その法人が存在しなければ架電しても計上できない。
9. **「総額売上高」は会計上の売上高ではない（§3-7・§6-3）** — 親会社の決算説明会資料や
   サステナ説明会資料に載る子会社の「総額売上高」はグロス指標で、会計上の売上高と乖離する。

### 非公開化・MBO・TOB は親子判定を変える
MBOやTOBで上場廃止になった会社は、**買収SPCの完全子会社**になっている。
その瞬間から §3-3 により連結値は使えず、単体値でしか判定できない。
実例: ラクスルは2026/3/10にTOB成立・上場廃止でSPCの完全子会社。
連結619.5億円は根拠にできず、正しくは単体518.21億円（2025年7月期・有報）。
1次は「連結が使える」と誤認したまま○に到達していた（結論は同じでも根拠が誤り）。
**単体が300億未満の会社で同じ誤認をすれば事故る。**
上場廃止・非公開化を見つけたら、必ず単体値で判定し直せ。

### 規模の整合チェック（決算公告に売上高が無くても使える）
決算公告が貸借対照表のみで売上高を載せていなくても、**純利益・利益剰余金・総資産は
主張値の妥当性を判定できる。** 売上400億円規模の事業会社が純利益125万円・
利益剰余金3,321万円ということは通常ない。
実例: キスケの官報決算公告（2025年3月31日決算）は純利益125万1,000円・
利益剰余金3,321万3,000円で、主張値400億円台の事業会社とは整合しない。
逆に規模が整合すれば、数値そのものを再現できなくても○を支持する材料になる。

### 上限チェック（子会社の主張値を潰す強力な手）
子会社の採用値が疑わしいとき、**親会社のセグメント売上高（会計ベース）と突き合わせろ。**
そのセグメントには他社も含まれるので、**子会社単体はセグメント合計を超えられない。**
実例: エムアイカードの主張値346億円に対し、三越伊勢丹HDのクレジット・金融・友の会業
セグメントは同期344.33億円。セグメント全体が主張値を下回る＝単体346億は算術的にありえない。
この矛盾で1次値がグロス指標由来だと露見した。

## 絶対にやるな
数字をひねり出すこと。§6-6の通り誤った数値が禁止ソース（就活会議/SalesNow/Baseconnect/
マイナビ/doda/OpenWork等）に広く流通しており、WebSearchの要約はそこから合成されうる。
承認ソースの記載として確認できないなら「保留」にしろ。**推測を書けば失敗だ。**
アポインターは成果報酬で稼働している。誤って○にすれば無報酬になる（§1）。

## I列の書式（次のいずれかで始める）
- 「○ 妥当：〜」        既存のG列H列が正しく判定もルール通り。**架電してよい**
- 「○ 妥当（G列訂正：〜）」 架電可否は○のままだが、**G列の数値・決算期・単体連結の別が誤り**。
  正しい値と根拠を書く。300億を超えているかどうかと、書かれている数字が正しいかは別問題だ。
  数値が承認ソースで再現できず禁止ソースの値と一致する場合もここに該当する。
- 「× 要修正：〜」      条件を満たさないことが確定。正しい内容を書く
- 「△ 要再確認：〜」    ルール違反の疑い（子会社に連結値、catr.jp単独、鮮度切れ等）
- 「保留 検証不可：〜」  承認ソースに到達できず真偽判定できない。当たった経路を列挙

**社名変更・合併（§6-7）を見つけたら必ず I列に書け。** 商号が変わっていればアポインターが
架電時に名乗る社名が存在しない。実例: 行849 IHIインフラシステム→2026/4/1 IHIインフラスクエア。

## J列
検証に使ったURL。セミコロン区切り。**存在しないURLを創作するな。**
WebSearchの結果に出たURLのみ。到達できなければ空文字。`

const RULESET = IS_T3 ? RULESET_T3 : RULESET_OK

phase('Verify')
const results = await pipeline(
  Array.from({ length: NB }, (_, i) => i),
  (b) => agent(
    `TOKIUM「直アポ大」リストのG列・H列が本当に正しいかを検証する。層=${TIER}。

## 最初に読め
1. ${RULES} を全文読め。
2. ${SP}/waves/w${ws}_b${b}.json ← 担当行。

${CONSTRAINTS}

${IS_T3 ? RULESET_T3 : RULESET_OK}

## 出力
${SP}/waves/out_w${ws}_b${b}.json に JSON 配列で書け。**担当行数ぴったり・入力と同じ順序**。
各要素: {"行": <整数>, "I": "<I列>", "J": "<J列>"}
python3 のヒアドキュメントで書き、書いた後に読み直して要素数が入力と一致することを確認しろ。
行数がずれると全行ずれる（RULES §5-2 の事故が実際に起きている）。

## 返り値
${IS_T3 ? `{block:${b}, rows_written:<実際の要素数>,
 flipped_rows:[**△から○に回収した行**（＝架電可に戻した行）],
 kept_ok_rows:[**同上。回収した行をここにも入れる**（後段が回収の妥当性を検証する）],
 thin_margin_rows:[**△のまま残したが、まだ当たっていない経路があり回収の見込みがある行**],
 unverified_rows:[検索枠切れで検証できなかった行],
 searches_used:<WebSearch呼出回数>, notes:"<簡潔に>"}` : `{block:${b}, rows_written:<実際の要素数>, flipped_rows:[元○から覆した行],
 kept_ok_rows:[○のまま妥当とした行],
 thin_margin_rows:[**採用値が450億円未満**、または数値・決算期・親子判定に疑いが残る行],
 unverified_rows:[検索枠切れで検証できなかった行],
 searches_used:<WebSearch呼出回数>, notes:"<簡潔に>"}`}`,
    { label: `w${ws}:blk${b}`, phase: 'Verify', schema: SCHEMA }
  ),
  (res, b) => {
    if (!res || !res.kept_ok_rows || res.kept_ok_rows.length === 0) {
      return { block: b, overturned: [], summary: '反証対象なし' }
    }
    // 反証は「基準ギリギリ」「疑いが残る」行に絞る。3,000億円の行を叩いても覆らない。
    const thin = new Set(res.thin_margin_rows || [])
    const targets = IS_T3
      ? Array.from(new Set([].concat(res.kept_ok_rows || [], res.thin_margin_rows || [])))
      : res.kept_ok_rows.filter((r) => thin.has(r))
    if (targets.length === 0) {
      return { block: b, overturned: [], summary: '対象なし' }
    }
    return agent(
      `ブロック${b}で「○ 妥当＝架電してよい」と判定された行を**反証しに行く**。

${RULES} を読め。
1次結果: ${SP}/waves/out_w${ws}_b${b}.json
入力:     ${SP}/waves/w${ws}_b${b}.json
反証対象の行: ${JSON.stringify(targets)}
**これらの行のみ**扱え。既に△/×/保留の行は触るな。
これらは1次が「○だが基準300億に対し余裕が薄い、または数値・決算期・親子判定に疑いが残る」
と判定した行だ。採用値が基準を桁違いに超える行は対象から外してあるので、
限られた検索枠をここに集中できる。

${CONSTRAINTS}

${IS_T3 ? `## 姿勢（この層は二方向）
対象行は2種類ある。**I列の先頭を見て使い分けろ。**

**(a) 「○ 回収」で始まる行 — 厳しく反証しろ。**
1次が△から○に戻した行だ。回収が誤っていればアポインターが架電して無報酬になる（§1）。
- 見つけたという承認ソースは本当に存在し、その数値がそこに書かれているか
- 子会社にグループ値・連結値を当てていないか（§3-3/§6-4）
- 決算期は直近3期以内か（§3-8）。前年度値を掴んでいないか
- 禁止ソースの数値を承認ソースと誤認していないか（§6-6）
反証できたら △ に戻せ。

**(b) 「△」で始まる行 — もう一度だけ回収を試みろ。**
1次が「まだ当たっていない経路がある」と判断した行だ。
1次のI列に列挙された経路を読み、**そこに無い経路を狙え。**
特に §5-3 の 5（決算公告）と 6（親会社IRの巻末「関係会社の状況」「主要な損益情報等」）。
見つからなければ △ のままでよい。**推測で○に戻すな。**` : `## 姿勢
追認が仕事ではない。**この○は間違っているという仮説から始めろ。**`}
- 親会社と判断した根拠は何か。登記上の親会社が別にいないか。
- 採用値は本当に「単体」か。脚注に「グループ」「連結」の注記はないか。
- 決算期は直近3期以内か。前年度の値を掴まされていないか。
- 出典URLは実在し、その数値がそこに書かれているか。
- 法人番号は一致するか。同名の別法人ではないか。

**確信を持てた行だけ**判定記号を書き換えろ。確信がなければ1次の判定のまま残せ。
${IS_T3 ? '推測で○に戻すな。同時に、証拠があるのに△のまま放置もするな。' : '反証できないことを理由に保留に倒すな（架電機会の損失になる）。'}

ただし**判定が○のままでも、G列の数値・決算期・単体連結の別が誤っていると分かったら
I列を「○ 妥当（G列訂正：〜）」に書き換えろ。** これは覆しではないが重要な成果だ。
実例: 日鉄スラグ製品は1次の515.95億円が自社IRに存在せず正しくは343.48億円だった。
「金額をシード指定して検索し、その値が返らず別の整合的な数表が返る」のは1次値が誤りである
強い信号だ。この手を使え。
検索残量は逼迫している。疑いの濃い行から使え。

## 出力
覆した行だけ ${SP}/waves/out_w${ws}_b${b}.json を書き換えろ（要素数・順序厳守）。

## 返り値
{block:${b}, overturned:[覆した行番号], summary:"<根拠。覆せなかった行に残る疑問も書け>"}`,
      { label: `adv${ws}:blk${b}`, phase: 'Adversarial', schema: ADV_SCHEMA }
    )
  }
)

return { wave: W, tier: TIER, blocks: results.filter(Boolean) }
